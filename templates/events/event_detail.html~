<!-- templates/events/event_detail.html (объединенная форма) -->
{% extends 'base.html' %}

{% block title %}{{ event.name }} - Путь к успеху{% endblock %}

{% block page_title %}Информация о конкурсе{% endblock %}

{% block page_actions %}
    <a href="{% url 'events:list' %}" class="btn btn-secondary">
        <i class="bi bi-arrow-left"></i> Назад к списку
    </a>
    <a href="{% url 'events:add_participants' event.pk %}" class="btn btn-success">
        <i class="bi bi-person-plus"></i> Добавить участников
    </a>
    <a href="{% url 'events:update' event.pk %}" class="btn btn-warning">
        <i class="bi bi-pencil"></i> Редактировать
    </a>
{% endblock %}

<!-- templates/events/event_detail.html (обновляем секцию участников) -->
{% block content %}
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">Основная информация</h5>
            </div>
            <div class="card-body">
                <table class="table">
                    <tr>
                        <th>Название:</th>
                        <td>{{ event.name }}</td>
                    </tr>
                    <tr>
                        <th>Описание:</th>
                        <td>{{ event.description|linebreaks }}</td>
                    </tr>
                    <tr>
                        <th>Уровень:</th>
                        <td>{{ event.get_level_display }}</td>
                    </tr>
                    <tr>
                        <th>Срок подачи заявок:</th>
                        <td>{{ event.application_deadline|date:"d.m.Y"|default:"Не указан" }}</td>
                    </tr>
                    <tr>
                        <th>Дата объявления результатов:</th>
                        <td>{{ event.result_date|date:"d.m.Y"|default:"Не указана" }}</td>
                    </tr>
                    <tr>
                        <th>Статус:</th>
                        <td>
                            {% if event.is_active %}
                                <span class="badge bg-success">Активен</span>
                            {% else %}
                                <span class="badge bg-secondary">Неактивен</span>
                            {% endif %}
                        </td>
                    </tr>
                    <tr>
                        <th>Порядок сортировки:</th>
                        <td>{{ event.sort_order }}</td>
                    </tr>
                    <tr>
                        <th>Дата создания:</th>
                        <td>{{ event.created_at|date:"d.m.Y H:i" }}</td>
                    </tr>
                    <tr>
                        <th>Последнее обновление:</th>
                        <td>{{ event.updated_at|date:"d.m.Y H:i" }}</td>
                    </tr>
                    {% if event.created_by %}
                    <tr>
                        <th>Создал:</th>
                        <td>{{ event.created_by.get_full_name|default:event.created_by.username }}</td>
                    </tr>
                    {% endif %}
                    <tr>
                        <th>Количество участников:</th>
                        <td>
                            <span class="badge bg-primary">{{ participants_count }}</span>
                        </td>
                    </tr>
                </table>
            </div>
        </div>

        <!-- Список участников -->
        <div class="card mt-4">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="bi bi-people"></i> Участники конкурса
                    {% if participants %}
                        <span class="badge bg-primary float-end">{{ participants.count }}</span>
                    {% endif %}
                </h5>
            </div>
            <div class="card-body">
                {% if participants %}
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>№</th>
                                    <th>Ребенок</th>
                                    <th>Студия</th>
                                    <th>Педагог</th>
                                    <th>Результат</th>
                                    <th>Дата отчета</th>
                                    <th>Действия</th>
                                </tr>
                            </thead>
                            <!-- templates/events/event_detail.html (обновляем секцию действий в таблице участников) -->
<tbody>
    {% for participant in participants %}
        <tr>
            <td>{{ forloop.counter }}</td>
            <td>
                <strong>{{ participant.child.fio }}</strong>
            </td>
            <td>{{ participant.enrollment.studio.name }}</td>
            <td>{{ participant.enrollment.teacher }}</td>
            <td>
                {% if participant.result_type %}
                    <span class="badge bg-success">{{ participant.result_type.name }}</span>
                {% elif participant.custom_result %}
                    <span class="badge bg-warning">{{ participant.custom_result }}</span>
                {% else %}
                    <span class="badge bg-secondary">Без результата</span>
                {% endif %}
            </td>
            <td>{{ participant.report_date|date:"d.m.Y" }}</td>
            <td>
                <div class="btn-group" role="group">
                    <!-- Просмотр карточки участия -->
                    <a href="{% url 'participation:detail' participant.pk %}"
                       class="btn btn-sm btn-outline-info"
                       title="Просмотр участия">
                        <i class="bi bi-eye"></i>
                    </a>

                    <!-- Иконки просмотра файлов -->
                    {% with files=participant.uploadedfile_set.all %}
                        {% if files %}
                            {% if files.count == 1 %}
                                <!-- Если один файл - показываем кнопку просмотра -->
                                <a href="{% url 'participation:view_file' files.first.pk %}"
                                   class="btn btn-sm btn-outline-primary"
                                   title="Просмотр файла: {{ files.first.original_name }}"
                                   target="_blank">
                                    <i class="bi bi-file-earmark-text"></i>
                                </a>
                            {% elif files.count > 1 %}
                                <!-- Если несколько файлов - показываем кнопку с dropdown -->
                                <div class="btn-group" role="group">
                                    <button type="button"
                                            class="btn btn-sm btn-outline-primary dropdown-toggle"
                                            data-bs-toggle="dropdown"
                                            aria-expanded="false"
                                            title="Просмотр файлов ({{ files.count }})">
                                        <i class="bi bi-file-earmark-text"></i> {{ files.count }}
                                    </button>
                                    <ul class="dropdown-menu">
                                        {% for file in files %}
                                            <li>
                                                <a class="dropdown-item"
                                                   href="{% url 'participation:view_file' file.pk %}"
                                                   target="_blank">
                                                    <i class="bi bi-file-earmark-text"></i>
                                                    {{ file.original_name }}
                                                </a>
                                            </li>
                                        {% endfor %}
                                    </ul>
                                </div>
                            {% endif %}
                        {% else %}
                            <!-- Если нет файлов - показываем неактивную кнопку -->
                            <button class="btn btn-sm btn-outline-secondary"
                                    disabled
                                    title="Нет файлов">
                                <i class="bi bi-file-earmark-text"></i>
                            </button>
                        {% endif %}
                    {% endwith %}
                </div>
            </td>
        </tr>
    {% endfor %}
</tbody>
                        </table>
                    </div>
                {% else %}
                    <div class="alert alert-info">
                        <i class="bi bi-info-circle"></i>
                        В этом конкурсе пока нет участников.
                        <a href="{% url 'events:add_participants' event.pk %}">Добавьте первых участников</a>.
                    </div>
                {% endif %}
            </div>
        </div>
    </div>

    <div class="col-md-4">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">Действия</h5>
            </div>
            <div class="card-body">
                <div class="d-grid gap-2">
                    <a href="{% url 'events:list' %}" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Назад к списку
                    </a>
                    <a href="{% url 'events:add_participants' event.pk %}" class="btn btn-success">
                        <i class="bi bi-person-plus"></i> Добавить участников
                    </a>
                    <a href="{% url 'events:update' event.pk %}" class="btn btn-warning">
                        <i class="bi bi-pencil"></i> Редактировать
                    </a>
                    <a href="{% url 'events:delete' event.pk %}" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Удалить
                    </a>
                </div>
            </div>
        </div>

        {% if participants %}
        <div class="card mt-3">
            <div class="card-header">
                <h5 class="card-title mb-0">Статистика</h5>
            </div>
            <div class="card-body">
                <p><strong>Всего участников:</strong> {{ participants_count }}</p>

                {% if result_stats %}
                <h6>Результаты:</h6>
                <ul class="list-unstyled">
                    {% for result, count in result_stats.items %}
                        <li>{{ result }}: <span class="badge bg-primary">{{ count }}</span></li>
                    {% endfor %}
                </ul>
                {% endif %}

                {% if studio_stats %}
                <h6>По студиям:</h6>
                <ul class="list-unstyled">
                    {% for studio, count in studio_stats.items %}
                        <li>{{ studio }}: <span class="badge bg-success">{{ count }}</span></li>
                    {% endfor %}
                </ul>
                {% endif %}
            </div>
        </div>
        {% endif %}
    </div>
</div>
{% endblock %}